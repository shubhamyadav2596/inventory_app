# ---------- Server ----------
PORT=5001
# Comma separated list of allowed frontend origins (CORS)
ALLOWED_ORIGINS=http://localhost:5173

# ---------- Auth (Basic login) ----------
AUTH_USER=admin
AUTH_PASS=
JWT_SECRET=sffAweqihvIokddnkdnkiPqdfdMdjnfkdnHJdfjdij
JWT_EXPIRES_IN=1d

# ---------- PostgreSQL (Neon Cloud) ----------
# Get this from https://console.neon.tech -> your project -> Connection string
DATABASE_URL=

# ---------- Kafka (Confluent Cloud) ----------
# Get these from https://confluent.cloud -> Cluster -> API Keys
KAFKA_ENABLED=true
KAFKA_BROKER=
KAFKA_API_KEY=
KAFKA_API_SECRET=
KAFKA_TOPIC=inventory-events
KAFKA_GROUP_ID=fifo-worker-group
